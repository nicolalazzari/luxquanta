# Required to enable the password wall. If unset, the site stays public (no login).
DECK_PASSWORD=choose-a-strong-password

# Optional: separate secret for signing the session cookie (defaults to DECK_PASSWORD).
# Set a long random string in production if you want cookie forgery resistance even if the site password is weak.
# DECK_AUTH_SECRET=
